# 2026-08-26

## 搭建信息概览悬浮卡片（横版）布局调整
- `src/components/Construct/ConstructFloatCards.tsx` 横版（landscape，文件末尾 return 分支）：
  - 施工进程卡片竖向步骤滚动区 `maxHeight: 260 → 200`（卡片整体变矮）。
  - "施工进度"图例由竖向 4 个改 `grid grid-cols-2`（一行两个）。
  - 整体定位 `top: 320 → 230`（向上移）；图例 `gap-y-8 → gap-y-4`（上下间距变小）。
  - 注意：竖版(portrait，文件头部 return 分支)保持原样未动。用户最初误说"竖版"后纠正为横版。

## 空状态图放错位置修正（Exhibition/RightSidebar）
- 错误：订单面板(ordersSection, 水电气网络申报订单数量)的空态曾误用 `未报到展位汇总.png`；且「未报到展位汇总」面板(unreportedSection)本就漏配空态图。
- 修正：ordersSection 空态改为 `水电气网络申报订单数量.png`；unreportedSection 在 `unReportedRows.length===0` 时显示 `未报到展位汇总.png`。
- 易混淆点：Exhibition/RightSidebar 内有两面板——「未报到展位汇总」与「水电气网络申报订单数量(订单)」，空态图别串。modal/BoothModal 订单弹窗空态用的是 `水电气网络申报订单数量.png`（正确）。
- Exhibition/LeftSidebar「项目运营总览」面板(:102-150)原只有 loading 的 Spin、无数据空态；已补 `visibleRows.length===0` 时显示 `项目运营总览.png`。注：该文件 :154 第二个 PanelTitle 误写成"项目运营总览"，实为"特装展位报馆"面板（注释 :152 写明），未改。

## 空状态占位图替换（去掉文字，换成 public/img/empty 下的图）
- 需求：所有"暂无xxx"空状态文字去掉，改用对应的业务空状态插画图（图自带文案），图片在该区域内水平+垂直居中、按比例自适应（objectFit: contain）。
- 已替换（文字→图）：
  - Construct/LeftSidebar：进程总览步骤(暂无进程数据)→搭建进程总览.png；process overview 面板→搭建进程总览.png；明细(暂无明细数据)→搭建进度明细.png。
  - Construct/ConstructOverviewChart(暂无数据)→搭建进度统计.png。
  - Construct/ConstructCarousel / Safety/ConstructCarousel(暂无图片)→现场图片空.png。
  - Exhibition/RightSidebar(未报到展位汇总 暂无数据)→未报到展位汇总.png。
  - Safety/LeftSidebar：查处违规汇总(暂无数据)→查处违规汇总.png；违规记录(暂无数据)→现场违规记录.png。
  - modal/BoothModal(暂无订单信息)→水电气网络申报订单数量.png。
  - Construct/modal/BoothModal(暂无历史进程)→搭建进度明细.png。
  - CenterMap(暂无展位数据,原 antd Empty)→项目运营总览.png（并移除未用的 Empty import）。
  - HallOverviewMap(暂无馆级总览数据)→项目运营总览.png。
- 保留 switching 分支：Construct/LeftSidebar 两处切换中仍显示"正在切换数据..."文字。
- **补齐（用户指出之前漏做/弄错）**：在 Exhibition/RightSidebar 特装费用缴纳面板、`totalBooth===0` 时显示 `特装费用缴纳.png`；在 Safety/RightSidebar 违规风险等级面板、`riskDates.length===0` 时显示 `违规风险等级.png`。现已全部 11 张空状态图就位、无遗漏。
- 注：DashboardHeader 标题"展会概况总览"只是页眉标题、非数据面板，无空态可配，无需配图。用户之前消息"展会概况的"之后断开，实际意图即确认未报到展位汇总.png 归属，已落地。
- 基线错误未触碰：ConstructOverviewChart.tsx:58、Safety/LeftSidebar.tsx:186 的 Highcharts `as` 类型错(ts 2352)；CenterMap.tsx:397 类型谓词错(ts 2677)，均项目既有。
