# 2026-08-20

## 安全违规详情弹窗 - 违规记录幽灵样式改造
- 文件：`src/components/Safety/modal/BoothModal.tsx` + `index.css`
- 需求：`违规记录` 列表每条顶部用幽灵样式显示整改状态（`整改`/`限时整改` 等，字段 `safetyStatus`）+ 风险等级（`riskAssessment`），不再用带背景填充的标签
- 新增 `.construct-ghost-tag` 样式：透明背景 + `1px currentColor` 描边 + 圆角 + 文字颜色，白色/风险色均可
- 记录条目改为：顶部幽灵标签 → 违规内容（`recordContent`）→ 创建人（`createBy`）/ 整改时间（`targetCheckTime`）
- 后续：去掉弹窗底部"现场图片"分区（删除 `collectImages` 函数、`images` 变量），保留违规信息/违规内容/违规记录三区
- 违规记录样式再调整：整改状态标签蓝色`#60A5FA`；风险等级色 一般=黄`#FA8C16`、较大/重大/严重=红`#F5222D`、其他=蓝；幽灵标签字号13px内边距加大，违规内容15px，创建人/整改时间13px且间距加大（gap-x-5）
- **风险等级色更正**：以 `SafetyFloatCards.tsx` 的 `RISK_LEGEND` 为准——一般风险=蓝`#2563EB`、较大风险=黄`#FA8C16`、严重风险=红`#F5222D`；BoothModal 已同步该映射（严重→红、较大/重大→黄、其他→蓝）
- 横竖版切换控件由 `<select>` 替换为两张图片按钮（`/img/选中-横板.png`/`未选中-横板.png`/`选中-竖版.png`/`未选中-竖版.png`），按钮高30px，宽按图自适应，aria-pressed 反映状态，点击未选中项才触发切换
- 图片重命名：中文→英文（landscape-unselected/selected、portrait-unselected/selected），App.tsx 引用已改英文；PowerShell 中文参数编码乱码，改用 node .mjs 脚本完成
- Construct/LeftSidebar 搭建进度明细参考 Exhibition/LeftSidebar 样式：表头、行单元格全部居左（去掉 text-center/justify-center），表头 py-1.5→py-2、字号14px、颜色改 white/0.8、保留 bg-list.png 背景
- ConstructFloatCards 横版两张浮动卡片（施工进程/搭建进度）宽度统一为 220px 对齐（原搭建进度 180）
- HallMap 底图不再叠加任何文字：`showCode` 与 `showName` 均设为 false（之前只关了展位号，企业名称仍被 fillText 画出导致"底图显示名字"），底图只保留色块+点击/hover
