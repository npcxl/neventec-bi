# 2026-08-10

## Git 提交
- 提交 `fe881b1`: feat: 统一安全/搭建/展会模块样式，重构安全总览页
- 20 files changed, 1999 insertions(+), 714 deletions(-)
- 主要内容: 安全总览 Highcharts 3D 饼图、SafetyFloatCards、BoothModal、现场图片轮播、antd Steps/Card 重写 ConstructFloatCards、统一颜色方案、展馆选择滚动按钮、mock 数据

## 展会概况地图改造：ECharts → SVG Overlay
- 安装 `react-zoom-pan-pinch` 依赖
- 新增 `src/components/HallMap/` 目录：
  - `types.ts` — HallData、Booth 类型定义
  - `utils/constants.ts` — 颜色/透明度/缩放常量
  - `utils/transform.ts` — mock JSON → HallData adapter（bbox → polygon）
  - `HallImage.tsx` — 背景图 SVG 组件
  - `BoothLayer.tsx` — SVG polygon 展位覆盖层（保留备用）
  - `index.tsx` — 主容器，整合 react-zoom-pan-pinch + SVG overlay
- 修改 `CenterMap.tsx`：
  - 删除 DemoChart（ECharts custom series 渲染，约 480 行代码）
  - 删除 SafetyCoordItem/SafetyCoordResponse 类型和 normalize 函数
  - 替换为 HallMap 组件 + mock 数据适配
  - hallIndex=0 → 北京方案展, hallIndex=1 → 首钢会展中心4号馆1层
- 展位点击事件调用 handleBoothClick → handleSelect → 打开原有 BoothModal
- vite build 成功，无类型错误

