## 2025-08-05

### 修复 Highcharts 3D 模块导入错误
- `ConstructOverviewChart.tsx`: Highcharts 13 中 `highcharts-3d` ESM 导出不再是可调用函数，改为 side-effect import (`import 'highcharts/esm/highcharts-3d.src.js'`)

### ConstructFloatCards 在横屏模式显示
- `RightSidebar.tsx` landscape 分支: 在顶部添加 `ConstructFloatCards`，下方保留 overview + carousel 双列 grid

### 统一 landscape 模式地图/右侧比例
- `App.tsx`: 展会概况和安全总览 landscape 模式 `grid-rows` 从 `55%_minmax(0,1fr)` 改为 `70%_minmax(0,1fr)`，与搭建信息保持一致
