# 2026-08-25

## 回滚：图表"空数据显示0/空环"需求
- 用户决定回滚"无数据也渲染空环/显示0"的需求，恢复为"无数据→显示暂无数据整块替换"。
- 用 `git checkout HEAD --` 回滚了 4 个文件：
  - `src/components/Construct/ConstructOverviewChart.tsx`（恢复 `if(!hasData) 暂无数据`）
  - `src/components/Construct/ConstructProgressPie.tsx`（恢复原始，无占位扇区）
  - `src/components/Construct/RightSidebar.tsx`（恢复 `entries.every(e=>e.count===0)` 暂无数据三元）
  - `src/components/Safety/LeftSidebar.tsx`（恢复查处违规汇总暂无数据分支）
- 注意：这 4 个文件 HEAD 基线里 `as Highcharts.Options` 有 TS 2352 预存错误（Vite/esbuild dev 不阻断，仅 tsc 报错），属项目基线问题，非本次引入。
- 用户偏好：空数据直接显示"暂无数据"，不要空环占位。

## 现场安全总览地图颜色改为走 checkDrawings/summary/list 的 riskAssessment
- 用户要求：现场安全总览（SafetyOverview）地图颜色走 `a/api/checkDrawings/summary/list` 接口返回的 `riskAssessment`（审图风险评级），取适配 RISK_LEGEND 三级颜色（一般/较大/重大 = 蓝/橙/红）。
- 改动：`useBoothColorStrategy.ts` 的 `createSafetyOverviewStrategy` 改为以 `checkDrawingsSummary` 为主数据源取色；CenterMap 把 `checkDrawingsSummary` 透传进 hook（prop 类型补 `riskAssessment`/`boothId`）；新增 `SAFETY_RISK_FILL` 半透明填充色。
- 深排修复 2 个真实 bug：
  1. `normalizeRiskLevel` 原 medium 分支含 `v.includes('中')`，导致「重大风险」被误判为「较大风险」(橙)。改为 high 分支前置 + medium 用「中风险/中等」精确匹配。
  2. `App.tsx` 的 `refreshFromCache` 漏恢复 `checkDrawingsSummary`，切馆命中 60s 缓存后地图颜色/图标不更新。已补 `setCheckDrawingsSummary`。
- 隐患图标(riskBoothNos)与地图颜色同口径，基于 checkDrawingsSummary 的 riskAssessment。

## 澄清：getExhibitionProcess 与 getExhibitionProcessDetail 是两个不同接口
- 用户确认两者是**不同接口、提供不同数据**，并无矛盾：
  - `getExhibitionProcess`（+`getExhibitionProcessByHallId`）= 搭建进程悬浮卡片 `ConstructFloatCards` 的数据源（之前切回是对的）。
  - `getExhibitionProcessDetail` = 搭建进程**总览**的"当前/全部"数据（现在的需求，单独接口）。
- 结论：之前的接口切换与当前需求都正确，不要再把二者当成同一接口反复横跳。
