# 2026-08-25

## 回滚：图表"空数据显示0/空环"需求
- 用户决定回滚"无数据也渲染空环/显示0"的需求，恢复为"无数据→显示暂无数据整块替换"。
- 用 `git checkout HEAD --` 回滚了 4 个文件：
  - `src/components/Construct/ConstructOverviewChart.tsx`（恢复 `if(!hasData) 暂无数据`）
  - `src/components/Construct/ConstructProgressPie.tsx`（恢复原始，无占位扇区）
  - `src/components/Construct/RightSidebar.tsx`（恢复 `entries.every(e=>e.count===0)` 暂无数据三元）
  - `src/components/Safety/LeftSidebar.tsx`（恢复查处违规汇总暂无数据分支）
- 注意：这 4 个文件 HEAD 基线里 `as Highcharts.Options` 有 TS 2352 预存错误（Vite/esbuild dev 不阻断，仅 tsc 报错），属项目基线问题，非本次引入。
- 用户偏好：空数据直接显示"暂无数据"，不要空环占位。
