# 2026-08-05 工作日志

## 项目审查与 Skill 盘点

- 用户要求审查 neventec-bi 项目并查看项目 skills。
- 项目技术栈：React 19 + TS + Vite 6 + AntD 6 + ECharts 5 + GSAP 3 + Three.js + TailwindCSS 3。
- 项目仅含 1 个 skill：`.codebuddy/skills/neventec-bi-dashboard`（含 SKILL.md + references/api_reference.md + references/architecture.md）。
- 仓库根存在一份性能审查报告 `neventec-bi-performance-audit-and-cursor-prompt.md`，列出 P0~P2 共 9 类问题。
- 当前 git 工作区有未提交修改：App.tsx、Construct/RightSidebar.tsx、package.json、package-lock.json。
- 关键代码体量：App.tsx 1898 行、CenterMap.tsx 1748 行，单 chunk `dist/assets/index-*.js` 约 2.3MB（gzip 前），dist 总计 7.9MB。
- 最新提交：38ffb1e（四等分扇区恢复），性能优化提交 35560d5 已落地部分改动。

## 搭建总览 → Highcharts 3D Donut

- 安装 `highcharts@13.0.0`。
- 新建 `src/components/Construct/ConstructOverviewChart.tsx`，使用 Highcharts 3D pie/donut 替换原有 ECharts GL surface 环形图。
- 每个分片根据 count 值计算不同 depth（18~55），搭建正常 & 异常高度不同。
- 修改 `ConstructRightSidebar.tsx`：移除 echarts/echarts-gl 导入，改用新组件。
- `echarts-gl` 在项目其他位置无引用，已完全移除，减少构建体积。
- 构建通过，dist 单 chunk 约 2.65MB（gzip 850KB）。

## Highcharts 3D Donut 数据标签 + 深度可配置

- 开启 dataLabels：显示分片名称 + 数量，connector 连接线（`connectorShape: 'straight'`，半透明蓝色引线）。
- 添加配置 props：
  - `depthRange: [number, number]` — 等比映射深度范围（默认 [18, 55]）
  - `depthByLabel: Record<string, number>` — 按标签固定深度，如 `{ '搭建正常': 55, '严重滞后': 20 }`，优先级高于等比计算
  - `showDataLabels: boolean` — 是否显示连接线标签（默认 true）
  - `innerSize: string` — 环形内径%（默认 '55%'）
- 构建通过。
